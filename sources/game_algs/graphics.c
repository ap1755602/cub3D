#include "rendering.h"

void	graphics(t_game game)
{
	// int		x;
	// int		y;

	(void)game;
	// mlx_clear_window(game.mlx, game.window);

}

int	ft_update(t_game *game)
{
	graphics(*game);
	return (1);
}
